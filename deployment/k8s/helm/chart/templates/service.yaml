apiVersion: v1
kind: Service
metadata:
  name: {{ include "websocket-python-example.fullname" . }}
  labels:
    {{- include "websocket-python-example.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "websocket-python-example.selectorLabels" . | nindent 4 }}
